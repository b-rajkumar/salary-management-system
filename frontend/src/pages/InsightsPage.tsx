import { Stack, Typography } from '@mui/material';

export function InsightsPage() {
  return (
    <Stack spacing={3}>
      <Typography variant="h4">Insights</Typography>
      <Typography variant="body1">Coming with FR-5.</Typography>
    </Stack>
  );
}
